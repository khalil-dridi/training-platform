# MySQL connection for the MLA recommendation service
# Copy to .env and fill in your local values.
# Never commit real credentials.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=training_platform
DB_USER=root
DB_PASSWORD=
